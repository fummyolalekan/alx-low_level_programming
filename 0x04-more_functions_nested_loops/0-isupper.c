#include "main.h"

/**
 * _isupper - is a function used to check for uppercase alphabets
 * @c: an integer parameter
 * Return: something but success
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
